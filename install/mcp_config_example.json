{
  "_comment": "Snippet for ~/.config/Claude/claude_desktop_config.json on Linux/macOS, or %APPDATA%\\Claude\\claude_desktop_config.json on Windows. Copy the content inside the 'mcpServers' key of your config; do not overwrite the entire file.",
  "mcpServers": {
    "vivado": {
      "_comment": "Recommended: install the package first with 'pip install .' then use the CLI",
      "command": "vivado-mcp-server",
      "env": {
        "VMCP_HOST": "127.0.0.1",
        "VMCP_PORT": "7654",
        "VMCP_LOGLEVEL": "INFO"
      }
    },
    "vivado_from_source": {
      "_comment": "Alternative: run directly from the cloned repo using uv (no install needed)",
      "command": "uv",
      "args": ["run", "--project", "C:/path/to/vivado-mcp-server", "python", "-m", "server"],
      "env": {
        "VMCP_HOST": "127.0.0.1",
        "VMCP_PORT": "7654"
      }
    },
  }
}
